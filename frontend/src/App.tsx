// ---------------------------------------------------------------------------
// WORKSHOP STARTING POINT
// ---------------------------------------------------------------------------
// Placeholder. During slot 5/6 you will ask Gemini CLI (via @frontend-builder)
// to build out admin/attendee routes, Firebase Auth (Google SSO), poll voting
// UI, admin stats dashboard with Recharts, and an agent fetch helper in
// src/lib/agent.ts.
// Reference target lives in docs/reference/EXPECTED-App.tsx — facilitator only.
// ---------------------------------------------------------------------------

export function App() {
  return (
    <main style={{ fontFamily: 'system-ui', maxWidth: 720, margin: '40px auto', padding: 24 }}>
      <h1>Workshop Pulse</h1>
      <p>Empty shell. Build me with Gemini CLI.</p>
    </main>
  );
}
